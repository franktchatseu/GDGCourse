export const ROOT = 'http://localhost:8000/';
export const SERVER = ROOT + 'api/';

export const LOGIN = SERVER + 'auth/token';
export const UPDATE_PASSWORD = SERVER + 'auth/updatepassword';

export const BLOG = SERVER + 'blogs';
